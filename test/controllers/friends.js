var chats = require('../../controllers/friends');
var chai = require('chai');
var expect = chai.expect;
var should = chai.should();

describe('controllers/friends.js', () => {
  before(done => { done() });
  after(done => { done() });
  it('should ', (done) => { done() });
});