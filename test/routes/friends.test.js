var friendsRouter = require('../../routes/friends');
var chai = require('chai');
var expect = chai.expect;
var should = chai.should();

describe('routes/friends.test.js', () => {
  before(done => { done() });
  after(done => { done() });
  it('should ', (done) => { done() });
});