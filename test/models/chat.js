var chat = require('../../models/chat');
var chai = require('chai');
var expect = chai.expect;
var should = chai.should();

describe('models/chat.js', () => {
  before(done => {});
  after(done => {});
  it('should ', (done) => {});
});